import React from "react";
import { Image, Text, View } from 'react-native';

const MisMomentos = () => {
    return (
      <View style={{flex: 1, flexDirection: 'row',justifyContent:'space-between'}}>
       <Text>Mis momentos</Text>
      </View>
    );
}

export default MisMomentos;